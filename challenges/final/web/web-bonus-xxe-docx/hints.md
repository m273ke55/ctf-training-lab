# Подсказки

## Подсказка 1

DOCX — это ZIP-контейнер, внутри которого находится `word/document.xml`.

## Подсказка 2

XML-парсер разрешает DTD и внешние сущности.

## Подсказка 3

Добавьте SYSTEM-сущность `file:///flag.txt` в `word/document.xml` и вставьте её в текст документа.
