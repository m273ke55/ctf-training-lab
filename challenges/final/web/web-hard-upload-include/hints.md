# Подсказки

## Подсказка 1

Расширение загруженного файла проверяется, но его содержимое — нет.

## Подсказка 2

Параметр `page` подключает локальный файл через PHP `include`.

## Подсказка 3

Загрузите PHP-код с разрешённым расширением и подключите `uploads/имя-файла` через `page`.
