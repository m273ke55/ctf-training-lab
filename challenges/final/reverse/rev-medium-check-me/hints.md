# Подсказки

1. **Подсказка 1 — мягкая.** Не подбирайте флаг вручную: посмотрите, как программа проверяет введённую строку.
2. **Подсказка 2 — инструмент или подход.** Откройте `check_me.py` и найдите константы `KEY` и `DATA`.
3. **Подсказка 3 — почти раскрывает метод.** Каждый символ флага восстанавливается обратной операцией `chr(value ^ KEY)` для чисел из `DATA`.
