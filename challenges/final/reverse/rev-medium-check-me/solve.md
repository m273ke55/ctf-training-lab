# Решение

Откройте `dist/check_me.py`, найдите `KEY` и `DATA`, затем восстановите каждый символ выражением `chr(value ^ KEY)`. `src/solve.py` делает это автоматически через AST без хардкода флага.

Флаг: `edu_ctf{check_me_ok}`.
