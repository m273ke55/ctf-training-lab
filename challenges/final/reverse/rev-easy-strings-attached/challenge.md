# Простая задача Reverse: строки прицепом

Перед вами безопасный локальный артефакт `dist/strings_attached.bin`. Запускать его не нужно: начните со статического анализа, определения типа файла и поиска читаемых строк.

Найдите корректный флаг формата `edu_ctf{...}`.
