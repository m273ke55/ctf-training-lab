# Подсказки

1. Сначала преобразуйте hex-строку обратно в bytes.
2. Single-byte XOR означает, что каждый байт шифртекста был XOR-нут одним и тем же ключом.
3. Переберите 256 ключей и оставьте результат, который начинается с `edu_ctf{` и заканчивается `}`.
