# Подсказки

1. **Подсказка 1 — мягкая:** Расширение файла может быть ложным; начните с проверки типа.
2. **Подсказка 2 — инструмент или подход:** Используйте `file` и посмотрите первые байты через `xxd`.
3. **Подсказка 3 — почти решение:** Это обычный текстовый файл с другой подписью в имени; после определения типа его можно прочитать как текст.
