# Подсказки

1. Откройте `tiny_checker.py` как обычный текстовый файл и найдите константы `SHIFT` и `DATA`.
2. В массиве лежат значения `ord(ch) + SHIFT` для каждого символа флага.
3. Чтобы восстановить символ, вычтите `SHIFT` из числа и примените `chr(...)`.
