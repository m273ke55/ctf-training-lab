# Подсказки

1. Попробуйте `strings dist/comment.wav` и поищите строки рядом с комментариями.
2. В RIFF/WAV могут быть chunks с текстовой metadata, например комментарий.
3. Флаг лежит в текстовой области файла и имеет формат `edu_ctf{...}`.
