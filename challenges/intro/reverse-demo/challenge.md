# Демонстрация Reverse: строки в blob

Перед вами безопасный локальный артефакт `dist/demo_blob.bin`. Запускать его не нужно: начните со статического анализа, определения типа файла и поиска читаемых строк.

Найдите корректный флаг формата `edu_ctf{...}`.
