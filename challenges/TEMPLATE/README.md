# Шаблон задачи

Скопируйте эту папку для новой задачи. Студентам выдаются только `challenge.md`, файлы из `dist/` либо URL запущенного Web-сервиса. `solve.md`, `flag.txt`, `src/`, Dockerfile и внутренняя конфигурация остаются у преподавателя.
